import { StyleSheet, Text, View } from 'react-native'

const PrimaryButton = ({ chlidren }) => {
  return (
    <View>
      <Text>{chlidren}</Text>
    </View>
  )
}

export default PrimaryButton

const styles = StyleSheet.create({})
